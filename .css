/* ==========================================================================
   ETHICHAWKS PLATFORM MODAL & SYSTEM INTERFACE UPDATES
   ========================================================================== */

/* 1. Base Modal Configuration */
.eh-system-modal {
    display: none; 
    position: fixed; 
    z-index: 999999; 
    left: 0; 
    top: 0; 
    width: 100%; 
    height: 100%; 
    overflow: auto; 
    background-color: rgba(0, 0, 0, 0.7); 
    font-family: sans-serif;
}

/* 2. Modal Content Box Structure */
.eh-modal-content {
    background-color: #111625; 
    color: #ffffff; 
    margin: 5% auto; 
    padding: 35px; 
    border: 1px solid var(--gold-light, #d4af37); 
    border-radius: 6px; 
    width: 90%; 
    max-width: 750px; 
    box-shadow: 0 4px 25px rgba(0, 0, 0, 0.5); 
    position: relative;
}

/* Specific sizing adjustments for large compliance documents */
#nhiAccreditationModal .eh-modal-content,
#wilburMethodModal .eh-modal-content {
    margin: 3% auto;
    padding: 40px;
    width: 95%;
    max-width: 850px;
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.6);
}

/* 3. Scrollable Text Compartment */
.eh-modal-scroll-body {
    max-height: 450px; 
    overflow-y: auto; 
    margin-top: 20px; 
    padding-right: 15px; 
    font-size: 15px; 
    line-height: 1.7; 
    text-align: left;
}

#disclaimerModal .eh-modal-scroll-body,
#cookieModal .eh-modal-scroll-body,
#termsModal .eh-modal-scroll-body {
    max-height: 380px;
    margin-top: 15px;
    font-size: 14px;
    line-height: 1.6;
}

/* 4. Interactive Components & Micro-Interactions */
.eh-modal-close-trigger {
    color: #aaa; 
    position: absolute; 
    top: 15px; 
    right: 20px; 
    font-size: 28px; 
    font-weight: bold; 
    cursor: pointer; 
    line-height: 1;
    transition: color 0.2s ease;
}

.eh-modal-close-trigger:hover {
    color: var(--gold-light, #d4af37);
}

.eh-compliance-trigger {
    color: var(--gold-light); 
    text-decoration: none; 
    font-size: 12px; 
    opacity: 1; 
    cursor: pointer;
}

/* 5. Deep Insight Containers */
.eh-framework-insight-box {
    background-color: #0b0e17; 
    border-left: 4px solid var(--gold-light, #d4af37); 
    border-radius: 4px; 
    padding: 22px; 
    margin: 25px 0;
}

/* 6. Action Elements Inside Content Boxes */
.eh-modal-cta-box {
    text-align: center; 
    margin-top: 40px; 
    padding: 30px; 
    background-color: #0b0e17; 
    border-radius: 4px; 
    border: 1px solid rgba(212, 175, 55, 0.15);
}

.eh-modal-cta-button {
    display: inline-block; 
    background: linear-gradient(135deg, #1a237e, #4a148c); 
    color: white !important; 
    text-decoration: none; 
    padding: 14px 30px; 
    font-weight: bold; 
    border-radius: 25px; 
    font-size: 14px; 
    letter-spacing: 0.5px; 
    box-shadow: 0 4px 15px rgba(26, 35, 126, 0.3);
    transition: transform 0.2s ease;
}

.eh-modal-cta-button:hover {
    transform: translateY(-1px);
}

/* 7. HAWK Side Panel & Interface Layers */
#ehAssistantPanel {
    position: fixed; 
    top: 0; 
    right: -450px; 
    width: 100%; 
    max-width: 420px; 
    height: 100%; 
    background-color: #111625; 
    box-shadow: -5px 0 25px rgba(0,0,0,0.5); 
    transition: right 0.3s cubic-bezier(0.25, 1, 0.5, 1); 
    display: flex; 
    flex-direction: column; 
    z-index: 999999; 
    border-left: 1px solid var(--gold-light, #d4af37); 
    font-family: sans-serif;
}

#ehAssistantOverlay {
    display: none; 
    position: fixed; 
    z-index: 999998; 
    left: 0; 
    top: 0; 
    width: 100%; 
    height: 100%; 
    background-color: rgba(0, 0, 0, 0.5);
}
